# Reflex is installed from the submodule (./reflex) by the run script
# The run script executes: pip install -e ./reflex
# This ensures we use the reflex submodule, not a pip version
#
# Other dependencies can be added below as needed
reflex==0.8.15.dev1

# Test dependencies
pytest>=8.4.2
pytest-cov>=7.0.0
coverage>=7.10.7